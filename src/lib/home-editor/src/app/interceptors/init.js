/**
 * Bootstrapping the HomeEditor.Interceptors module
 */
(function (angular) {
'use strict';

var dependencies = [
    'HomeEditor.Services'
];

angular.module('HomeEditor.Interceptors', dependencies);

})(angular);