HomeEditor.Views.Root Module
===========================

This module defines the root UI state for the HomeEditor app.

This is an abstract state. This state only provides a resolve that logs in as ops@homdna.com.